<% if(getDoctor.ifdoctor.offlineAppointments){ %>  
    <div class="allappointments">
        <div class="apppoinments"> 
            <h6>Todays Appointments</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %> 
                        <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>
                        
                        <hr class="divider">

                    </li>
                <% } %>
            </ul>
        </div>
    
        <div class="apppoinments">
            <h6>Tomorrows Appointments</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %>
                       <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>


                    </li>
                    <hr class="divider">
                <% } %>
            </ul>
        </div>
    
        <div class="apppoinments">
            <h6>Next Day</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %>
                       <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>

                    </li>
                    <hr class="divider">
                <% } %>
            </ul>
        </div>
    </div>
    
    <h3 class="vit-appoint-heding">Virtual Appointments</h3>
    
    <div class="allappointments">
        <div class="apppoinments">
            <h6>Todays Appointments</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %>
                       <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>

                    </li>
                    <hr class="divider">
                <% } %>
            </ul>
        </div>
    
        <div class="apppoinments">
            <h6>Tomorrows Appointments</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %>
                       <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>

                    </li>
                    <hr class="divider">
                <% } %>
            </ul>
        </div>
    
        <div class="apppoinments">
            <h6>Next Day</h6>
            <ul>
                <% for (appointment of getDoctor.ifdoctor.offlineAppointments) { %>
                    <li>
                        <% if (appointment.name) { %>
                            <strong>Name:</strong> <%= appointment.name %> 
                            
                        <% } %><br>
                        <% if (appointment.phone_no) { %>
                            <strong>Phone Number:</strong> <%= appointment.phone_no %><br>
                        <% } %>
                        <% if (appointment.reason) { %>
                            <strong>Reason:</strong> <%= appointment.reason %><br>
                        <% } %>
                        <% if (appointment.state) { %>
                            <strong>State:</strong> <%= appointment.state %><br>
                        <% } %>
                        <% if (appointment.city) { %>
                            <strong>City:</strong> <%= appointment.city %><br>
                        <% } %>
                        <% if (appointment.date) { %>
                            <strong>Date:</strong> <%= appointment.date %>
                        <% } %>
                       <% if(appointment.status == "pending"){ %>
                        <div>
                            <button class="approve-button">
                                <a href="/doctors/approve/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approve</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/del/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Ignore</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approve button to accept appointment </p>
                            
                        </div>
                        <% }else if(appointment.status == "Confirmed"){ %> 
                                <div>
                                <button class="approve-button">
                                <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Approved</a>
                            </button>
                           
                                <button class="ignore-button">
                                    <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                    Remove</a>
                                </button>
                                <br>
                                <br>
                                <p>Click Approved button to change status</p>
                        </div><% } else{%><div>
                            <button class="approve-button">
                            <a href="/doctors/change/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">Ignored</a>
                        </button>
                       
                            <button class="ignore-button">
                                <a href="/doctors/remove/<%=getDoctor.ifdoctor._id %>/appointment/<%= appointment._id %>" style="text-decoration: none;color:rgb(255, 255, 255);">
                                Remove</a>
                            </button>
                    </div><% } %>

                    </li>
                    <hr class="divider">
                <% } %>
            </ul>
        </div>
    </div>
<% } %>

